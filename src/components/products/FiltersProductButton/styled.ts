import { Button, Image, Typography } from "antd";
import styled from "styled-components";

export const FilterProductButton = styled(Button)`
  gap: 33px;
  display: flex;
  align-items: center;
  flex-direction: row-reverse;
  font-size: 13px;
  color: #212b36;
  font-weight: 400;
`;
