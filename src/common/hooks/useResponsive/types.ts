export type Breakpoint = "xs" | "sm" | "md" | "lg" | "xl" | "xxl";

export interface ResponsiveProps {
  xs?: boolean;
  sm?: boolean;
  md?: boolean;
  lg?: boolean;
  xl?: boolean;
  xxl?: boolean;
}
