import { NewLetters } from "./NewsLetters";
import { SiteMap } from "./SiteMap";

const Footer = () => {
  return (
    <div>
      <SiteMap />
      <NewLetters />
    </div>
  );
};

export default Footer;
